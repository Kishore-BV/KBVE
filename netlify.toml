# Netlify build configuration
[build]
  # Use npm instead of bun to avoid native module compatibility issues
  command = "npm run build"
  publish = "dist"

# Optional: Specify Node.js version for consistent builds
[build.environment]
  NODE_VERSION = "22.22.0"
